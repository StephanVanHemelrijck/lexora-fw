import { languages } from './language.ts';
import { onboarding } from './onboarding.ts';
import { user } from './user.ts';

export * from './api.ts';

export const api = {
  languages,
  onboarding,
  user,
};
